{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": ["**/.env"],
  "pipeline": {
    "dev": {
      "persistent": true,
      "cache": false
    },
    "db:migrate": {
      "inputs": ["src/schema.ts"],
      "outputs": ["migrations/**"],
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    },
    "lint": {},
    "lint:fix": {},
    "clean": {
      "cache": false
    },
    "typecheck": {
      "cache": false
    }
  },
  "globalEnv": [
    "CI",
    "DATABASE_HOST",
    "DATABASE_PASSWORD",
    "DATABASE_USERNAME",
    "NODE_ENV",
    "VERCEL",
    "VERCEL_URL"
  ]
}
